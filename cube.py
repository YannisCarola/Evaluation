import pygame
import vector2

import core


class Cube:
    def __init__(self,largeur=200,hauteur=200):
        self.largeur = 10
        self.hauteur = 10
        self.couleur = (0,255,0)
        self.position = vector2

    def show(self, screen):
        pygame.draw.angle(screen,self.couleur,self.position,self.taille)

    if core.memory("Boule") = cube
        else reset()